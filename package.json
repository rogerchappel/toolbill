{
  "name": "toolbill",
  "version": "0.1.0",
  "description": "Local-first CLI bill of materials for coding-agent logs and git changes.",
  "type": "module",
  "bin": {
    "toolbill": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "examples",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "smoke": "npm run build && node dist/cli.js summarize fixtures/openclaw-text.log && node dist/cli.js json fixtures/codex-jsonl.log",
    "package:smoke": "npm pack --dry-run",
    "release:check": "npm run check && npm test && npm run smoke && npm run package:smoke"
  },
  "keywords": [
    "agent-observability",
    "cli",
    "codex",
    "git",
    "jsonl",
    "local-first",
    "logs"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
